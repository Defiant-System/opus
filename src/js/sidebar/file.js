
// opus.spawn.sidebar.file

{
	init(parent) {
		// fast reference
		this.parent = parent;
	},
	dispatch(event) {
		let APP = opus,
			Self = APP.spawn.sidebar.file,
			pEl,
			el;
		// console.log(event);
		switch (event.type) {
			case "some-event":
				break;
		}
	}
}
