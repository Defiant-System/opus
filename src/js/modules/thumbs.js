
// opus.spawn.spawn.thumbs

{
	init() {
		
	},
	dispatch(event) {
		let APP = opus,
			Self = APP.spawn.thumbs,
			Spawn = event.spawn,
			name,
			value,
			pEl,
			el;
		switch (event.type) {
			// custom events
			case "toggle-thumbs":
				break;
		}
	}
}
