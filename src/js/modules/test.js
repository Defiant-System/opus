
let Test = {
	init(APP, Spawn) {

		// Spawn.el.find("layout").addClass("show-blank-view");
		Spawn.el.find("layout").addClass("show-sidebar-format show-sidebar-thumbs");
		
		Spawn.el.find("sidebar.format").addClass("show-slides");

	}
};
